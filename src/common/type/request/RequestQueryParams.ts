/**
 * Generic query parameter map for request filtering.
 *
 * Used to construct dynamic, filterable API queries.
 */
type RequestQueryParams = Record<string, any>;

export default RequestQueryParams;
