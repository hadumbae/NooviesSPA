/**
 * Query keys for single-theatre fetch operations.
 */
export const TheatreIDQueryKeys = [
    "fetch_theatre_by_id",
    "fetch_theatre_by_slug",
];

/**
 * Query keys for theatre list fetch operations.
 */
export const TheatreListQueryKeys = [
    "fetch_theatres_by_query",
    "fetch_paginated_theatres_by_query",
];
