// ToggleReviewPublicityAction.tsx

type ActionProps = {};

export const ToggleReviewPublicityAction = (
    {}: ActionProps
) => {
    return (
        <div>
            {ToggleReviewPublicityAction.name}
        </div>
    );
};