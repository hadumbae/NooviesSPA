// ResetReviewDisplayNameAction.tsx

type ActionProps = {};

export const ResetReviewDisplayNameAction = (
    {}: ActionProps
) => {
    return (
        <div>
            {ResetReviewDisplayNameAction.name}
        </div>
    );
};