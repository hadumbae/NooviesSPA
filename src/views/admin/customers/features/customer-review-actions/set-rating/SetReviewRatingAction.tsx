// SetReviewRatingAction.tsx

type ActionProps = {};

export const SetReviewRatingAction = (
    {}: ActionProps
) => {
    return (
        <div>
            {SetReviewRatingAction.name}
        </div>
    );
};