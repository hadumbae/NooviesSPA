// CustomerReviewPageContent.tsx

type ContentProps = {};

export const CustomerReviewPageContent = (
    {}: ContentProps
) => {
    return (
        <div>
            {CustomerReviewPageContent.name}
        </div>
    );
};