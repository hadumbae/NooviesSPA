import {ReactElement} from "react";

type ContentProps = {};

export function CustomerReviewLogsPageContent(
    {}: ContentProps
): ReactElement {
    return (
        <div>
            {CustomerReviewLogsPageContent.name}
        </div>
    );
}