// FetchByCodeLoader.tsx

type LoaderProps = {};

export const FetchByCodeLoader = (
    {}: LoaderProps
) => {
    return (
        <div>
            {FetchByCodeLoader.name}
        </div>
    );
};