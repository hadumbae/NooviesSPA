/**
 * @fileoverview Base URL constant for the Genre CRUD administrative API.
 * Centralizes the endpoint configuration to ensure consistent network requests
 * across the frontend application.
 */

/**
 * The base endpoint for Genre management operations.
 */
export const GenreCRUDBaseURL = `/api/v1/admin/genres/crud`;