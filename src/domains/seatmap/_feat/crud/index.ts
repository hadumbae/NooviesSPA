import {SeatMapCRUDBaseURL} from "@/domains/seatmap/_feat/crud/baseURL.ts";
import {create, destroy, find, findByID, paginated, query, update} from "@/domains/seatmap/_feat/crud/repository.ts";

export {
    SeatMapCRUDBaseURL,
    find,
    findByID,
    paginated,
    query,
    create,
    update,
    destroy,
}

