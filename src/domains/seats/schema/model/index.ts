import {Seat, SeatBaseSchema, SeatSchema} from "@/domains/seats/schema/model/SeatSchema.ts";
import {SeatDetails, SeatDetailsSchema} from "@/domains/seats/schema/model/SeatDetailsSchema.ts";

export {
    SeatBaseSchema,
    SeatSchema,
    SeatDetailsSchema,
}

export type {
    Seat,
    SeatDetails,
}

