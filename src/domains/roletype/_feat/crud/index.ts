import {RoleTypeCRUDBaseURL} from "@/domains/roletype/_feat/crud/baseURL.ts";
import {create, destroy, find, findByID, paginated, query, update} from "@/domains/roletype/_feat/crud/repository.ts";

export {
    RoleTypeCRUDBaseURL,
    find,
    findByID,
    paginated,
    query,
    create,
    update,
    destroy,
}

