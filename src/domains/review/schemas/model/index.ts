import {MovieReview, MovieReviewSchema} from "@/domains/review/schemas/model/MovieReviewSchema.ts";
import { MovieReviewDetails, MovieReviewDetailsSchema } from "@/domains/review/schemas/model/MovieReviewDetailsSchema.ts";

export {
    MovieReviewSchema,
    MovieReviewDetailsSchema,
}

export type {
    MovieReview,
    MovieReviewDetails,
}

