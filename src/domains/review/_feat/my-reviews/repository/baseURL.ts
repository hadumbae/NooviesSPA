/**
 * @fileoverview Base URL configuration for the user reviews management repository.
 */

/** The base endpoint for managing the current user's reviews. */
export const ManageMyReviewsBaseURL = "/api/v1/user/reviews";